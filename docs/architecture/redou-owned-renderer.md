# Redou-Owned Renderer

The renderer is a Redou workbench UI, not a Codex UI copy. It shows Codex-derived execution state while preserving Redou ownership of projects, tasks, rules, context, approvals, events, git, logs, and artifacts.

The page layer composes panels only. Complex state and UI should be split into focused component directories and state slices.
