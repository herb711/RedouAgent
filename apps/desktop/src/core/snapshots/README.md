# Snapshots

Snapshots contains derived view models built from Redou task metadata and runtime AgentEvent streams.

Add focused snapshot builders here when UI projections become reusable outside a single orchestrator module.

Do not persist primary execution state here. Codex event streams remain the source of truth.
