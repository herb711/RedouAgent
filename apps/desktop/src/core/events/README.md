# Events

Events contains Redou event helpers, filters, and typed projections over runtime-originated AgentEvent records.

Add event classification and projection helpers here when more than one orchestrator or renderer API needs them.

Do not create a Redou plan/todo engine here; plan and todo state must remain derived from Codex events.
