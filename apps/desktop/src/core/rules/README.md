# Rules

Rules modules will parse, validate, merge, and explain Redou project and task rule packs.

Add rule loading and validation here; context-facing assembly belongs in `core/context/rulesContextBuilder.cjs`.

Do not mix rule persistence, renderer editing UI, or Codex protocol parameter construction into this directory.
