'use strict';

function createProcessSpawner(options = {}) {
  void options;
  // TODO Phase 2: wrap child_process spawn for runtime clients.
  return {};
}

module.exports = { createProcessSpawner };
