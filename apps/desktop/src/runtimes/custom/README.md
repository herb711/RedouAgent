# Custom Runtime

Custom runtime support is a future extension point for user-defined engines.

Add registry descriptors, configuration validation, and adapter boundaries here when the feature starts.

Do not implement arbitrary command execution or permission bypasses in this scaffold.
