# Hermes Runtime

Hermes is retained as a legacy runtime scaffold during the Redou Workbench rewrite.

Add only adapter boundaries and future compatibility hooks here. The old implementation remains in `apps/desktop/src_legacy` and `vendor/hermes` remains in place.

Do not move old Hermes execution logic into this new runtime scaffold during Phase 1.
