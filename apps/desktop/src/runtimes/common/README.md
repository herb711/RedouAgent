# Runtime Common

Common runtime files define adapter shape, capabilities, events, registry behavior, availability descriptors, and shared mapping helpers.

Add cross-runtime contracts here only when at least two runtimes need them.

Do not place Codex-specific JSON-RPC, Hermes implementation details, or UI state here.
